version: '3.3'

services:
  nginx:
    build: ./nginx
    ports:
      - "8899:80"
    volumes:
      - ./nginx/logs:/var/logs/nginx/:rw
    networks:
      - frontend

  swoole:
    build: ./swoole
    ports:
      - "9501"
    tty: true
    networks:
      - frontend
      - backend

  cgi:
    image: bccq/php-build
    ports:
      - "9000"
    volumes:
      - ./cgi/code:/var/www
    networks:
      - frontend
      - backend

  nodejs:
    build: ./nodejs
    ports:
      - "3000"
    tty: true
    networks:
      - frontend
      - backend

  # mysql:
  #   image: "mysql"
  #   ports:
  #     - 3306
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=123456
  #   networks:
  #     - backend

  # redis:
  #   image: "redis"
  #   ports:
  #     - 6379
  #   networks:
  #     - backend

networks:
  frontend:
  backend: